

- name: Configure OSPFv2 for each process
  arista.eos.eos_ospfv2:
    config:
      processes:
        - process_id: "{{ item.process_id }}"
          passive_interface:
            default: true
          redistribute:
            - routes: connected
    state: merged
  loop: "{{ ospf_items }}"
  

